# Vetale Browser WPF - Експорт Дизайнів

## 📋 Опис

Ця папка містить експортовані XAML дизайни з Avalonia проекту Vetale Browser для використання у WPF проекті на .NET Framework 4.8.

## 📁 Структура папки

```
VetaleBrowserWPF/
├── Resources/
│   ├── Icons/                  # Всі іконки та зображення кнопок
│   │   ├── AddBookmarkButton.png
│   │   ├── AddTab.png
│   │   ├── Backbutton.png
│   │   ├── CloseButton.png
│   │   ├── CloseTabButton.png
│   │   ├── DefaultTabItem.ico
│   │   ├── ForwardButton.png
│   │   ├── HomeButton.png
│   │   ├── InstrumensButton.png
│   │   ├── MaxButton.png
│   │   ├── MinButton.png
│   │   ├── ReloadTabButton.png
│   │   ├── SettingsButton.png
│   │   └── VetaleSearchIcon.png
│   └── ButtonStyles.xaml       # Словник стилів для кнопок
├── Controls/
│   ├── TabControl.xaml         # Дизайн вкладки браузера
│   └── NavigationBar.xaml      # Дизайн навігаційної панелі
├── Pages/
│   └── BrowserPage.xaml        # Головна сторінка браузера
├── Windows/
└── MainWindow.xaml             # Головне вікно

```

## 🎨 Що включено

### 1. MainWindow.xaml
Головне вікно з:
- Прозорим фоном (AllowsTransparency=True)
- Власним стилем без стандартного заголовка
- Градієнтами (сірий та фіолетовий)
- Стилями для кнопок керування вікном

### 2. Controls/TabControl.xaml
Дизайн вкладки браузера з:
- Округленими верхніми кутами (CornerRadius="8,8,0,0")
- Кнопкою вимкнення звуку з іконкою динаміка
- Фавіконом (24x24px)
- Текстом назви з обрізанням
- Кнопкою закриття
- Градієнтами для активного/неактивного стану
- Hover ефектами

### 3. Controls/NavigationBar.xaml
Навігаційна панель з:
- Кнопками: Назад, Вперед, Оновити, Домівка
- Адресною строкою з:
  - Іконкою статусу безпеки
  - Текстом статусу
  - Полем вводу URL
  - Кнопкою додавання закладки
- Кнопками Інструменти та Налаштування
- Округленою адресною строкою (CornerRadius="18")
- Всіма hover ефектами

### 4. Pages/BrowserPage.xaml
Повна сторінка браузера з:
- Панеллю вкладок
- Кнопками додавання вкладки та нового вікна
- Кнопками керування вікном (мінімізувати, максимізувати, закрити)
- Навігаційною панеллю
- Областю веб-контенту

### 5. Resources/ButtonStyles.xaml
Словник стилів з:
- Всіма градієнтами (сірий, фіолетовий, помаранчевий)
- Стилями для кнопок навігації
- Стилями для іконок
- Стилями для вікон (закрити, мінімізувати, максимізувати)
- Стилем адресної строки
- Кольорами вкладок

## 🔄 Конвертація з Avalonia у WPF

### Основні зміни:

1. **Namespace**
   - Avalonia: `xmlns="https://github.com/avaloniaui"`
   - WPF: `xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"`

2. **Styles → Style Triggers**
   - Avalonia: `<Style Selector="Button:pointerover">`
   - WPF: `<Trigger Property="IsMouseOver" Value="True">`

3. **BoxShadow → DropShadowEffect**
   - Avalonia: `BoxShadow="0 4 8 0 #40000000"`
   - WPF: `<DropShadowEffect ShadowDepth="0" BlurRadius="8"/>`

4. **CornerRadius**
   - Працює однаково в обох

5. **Image Source**
   - Avalonia: `Source="/VetaleBrowser.UI/Sources/Icons/image.png"`
   - WPF: `Source="pack://application:,,,/Resources/Icons/image.png"`

## 🚀 Як використовувати

### Варіант 1: Створення нового WPF проекту

1. Створіть новий WPF проект (.NET Framework 4.8) у Visual Studio
2. Скопіюйте всі файли з цієї папки у ваш проект
3. Додайте файли до проекту через Solution Explorer
4. Підключіть ButtonStyles.xaml до App.xaml:

```xml
<Application.Resources>
    <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="Resources/ButtonStyles.xaml"/>
        </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
</Application.Resources>
```

### Варіант 2: Інтеграція в існуючий проект

1. Скопіюйте папку `Resources/Icons` у ваш проект
2. Скопіюйте потрібні XAML файли
3. Змініть namespace та x:Class відповідно до вашого проекту
4. Додайте посилання на зображення

## 🎯 Використання компонентів

### TabControl
```xml
<controls:TabControl Title="Моя вкладка" 
                     FaviconSource="путь_к_иконке"
                     IsActive="True"/>
```

### NavigationBar
```xml
<controls:NavigationBar x:Name="NavBar"/>
```

### У коді C#:
```csharp
// Додавання обробників подій
NavBar.BackButton.Click += OnBackClick;
NavBar.ForwardButton.Click += OnForwardClick;
NavBar.ReloadButton.Click += OnReloadClick;
NavBar.AddressBar.KeyDown += OnAddressBarKeyDown;
```

## 🎨 Кольорова схема

### Градієнти:
- **GrayGradient**: #E0E0E0 → #C0C0C0 (фон панелі вкладок)
- **PurpleGradient**: #A020F0 → #8612D0 (акценти)
- **OrangeGradient**: #FF6B00 → #8A2BE2 (кнопка нового вікна)
- **ActiveTabGradient**: #DDD6FE → #C4B5FD (активна вкладка)
- **InactiveTabGradient**: #F3F4F6 → #E5E7EB (неактивна вкладка)

### Основні кольори:
- **Фон навігації**: #FF6D00 (помаранчевий)
- **Hover навігації**: #FF8800
- **Pressed навігації**: #FF5500
- **Адресна строка**: #F3E8FF (світло-фіолетовий)
- **Текст адреси**: #4C1D95 (темно-фіолетовий)
- **Назва вкладки**: #D84315 (помаранчево-червоний)

## 📝 Примітки

1. **Іконки**: Всі іконки скопійовані з оригінального Avalonia проекту
2. **Pack URI**: У WPF використовується формат `pack://application:,,,/Resources/Icons/image.png`
3. **Fonts**: Шрифт "Segoe MDL2 Assets" для іконок Windows (мінімізувати, максимізувати, закрити)
4. **Rounded Corners**: BorderRadius підтримується через CornerRadius
5. **Transparency**: Для прозорого вікна потрібно WindowStyle="None" та AllowsTransparency="True"

## ⚠️ Важливо

- **Code-behind**: Для роботи контролів потрібно створити відповідні .cs файли з логікою
- **DataBinding**: Деякі властивості можуть потребувати налаштування через DataContext
- **Events**: Всі події потрібно підключити в коді C#
- **.NET Framework 4.8**: Переконайтесь, що у вас встановлено .NET Framework 4.8

## 🔧 Додаткові налаштування

### Для безшовної інтеграції:

1. **Додайте CefSharp** для відображення веб-контенту:
```
Install-Package CefSharp.Wpf -Version 119.4.30
```

2. **Створіть ViewModel** для керування станом:
```csharp
public class TabViewModel : INotifyPropertyChanged
{
    public string Title { get; set; }
    public string Url { get; set; }
    public bool IsActive { get; set; }
    // ...
}
```

3. **Налаштуйте Data Binding** у XAML

## 📧 Підтримка

Якщо у вас виникли питання або потрібна додаткова допомога з конвертацією, створіть issue в репозиторії проекту.

---

**Версія**: 1.0
**Дата експорту**: 26 грудня 2025
**Джерело**: Vetale Browser Avalonia → WPF Export

